#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int hi[5] = {1, 2, 3, 4, 5};

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T; cin >> T;
    while(T--) {
        int N; string S; cin >> N >> S;
        cout << 
        cout << hi[100];
    }
}
