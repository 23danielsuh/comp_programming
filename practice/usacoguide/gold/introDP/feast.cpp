#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

void solve() {
    int N, M; cin >> N >> M;
    vector<int> A(N); for(auto &x : A) cin >> x;
    
}

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    solve();
}