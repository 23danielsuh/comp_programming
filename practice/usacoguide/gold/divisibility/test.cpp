#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int a, b;
    tie(a, b) = make_pair(5, 3);
    tie(a, b) = make_pair(100, 300);
    cout << a << " " << b << "\n";
}

