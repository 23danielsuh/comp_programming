#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

const int MOD = 998244353;

void solve() {
    int N; cin >> N;
    //the number of permutations is n!, meaning that the states if you were to simulate this would be size n * n!
    //
}

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int T = 1;
    // cin >> T;
    for(int i = 1; i <= T; ++i) {
        solve();
    }
}

